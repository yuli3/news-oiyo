{
  "name": "news-oiyo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "collect": "node scripts/collect-news.mjs",
    "audit:trends": "node scripts/audit-trend-notes.mjs",
    "sync": "node scripts/sync-news.mjs",
    "update": "npm run collect && npm run audit:trends && npm run sync"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.0.0"
  }
}
